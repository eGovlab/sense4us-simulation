'use strict';

var mouseDownWare = require('mouse_middleware').handleDown,
    objectHelper  = require('object-helper');

function mouseDown(canvas, loadedModel, pos) {
    
}

module.exports = mouseDown;