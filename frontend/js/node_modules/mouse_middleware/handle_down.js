'use strict';

var middleware = require('middleware');

var leftDownWare = middleware([
    require('./features/handle_down/activate_linking'),
    require('./features/handle_down/activate_move_icon'),
    require('./features/handle_down/activate_move_node')
]);

var rightDownWare = middleware([
    require('./features/handle_down/activate_right_linking')
]);

module.exports = {
    0: leftDownWare,
    2: rightDownWare
};
