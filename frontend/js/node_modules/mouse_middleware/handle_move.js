'use strict';

var middleware = require('middleware');

var leftMoveWare = middleware([
    require('./features/handle_move/move_linker'),
    require('./features/handle_move/move_icon'),
    require('./features/handle_move/move_node'),
    require('./features/handle_move/pan_canvas')
]);

var rightMoveWare = middleware([
    require('./features/handle_move/move_linker')
]);

module.exports = {
    0: leftMoveWare,
    2: rightMoveWare
};