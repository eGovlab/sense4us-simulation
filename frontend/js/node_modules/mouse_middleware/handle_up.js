'use strict';

var middleware = require('middleware');

var leftUpWare = middleware([
    require('./features/handle_up/link'),
    require('./features/handle_up/deselect'),
    require('./features/handle_up/stop_linking'),
    require('./features/handle_up/stop_moving_icon'),
    require('./features/handle_up/select')
]);

var rightUpWare = middleware([
    require('./features/handle_up/link'),
    require('./features/handle_up/stop_linking')
]);

module.exports = {
    0: leftUpWare,
    2: rightUpWare
}
